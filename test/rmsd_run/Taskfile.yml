# github.com/go-task/task

version: '2'

vars:
  GREETING: Hello, World!

tasks:
  default:
    cmds:
      - task: test1
      - task: test2

  test1:
    cmds:
      -  vmd -dispdev text -e test1.tcl

  test2:
    cmds:
      -  vmd -dispdev text -e test2.tcl

